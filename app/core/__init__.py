"""triage_core - pure Python, zero I/O, deterministic, seeded.

Blueprint 20: "The crown jewel. All eight layers of clinical and queue logic, with
no framework, no database and no network. Deterministic and therefore testable and
reproducible.  This module is the product; everything else is packaging. It should
be extractable as a library."
"""
